const discord = require('discord.js')
const fs = require('fs')

exports.run = (client, message) => {
  //   if(message.channel.name.toLowerCase() === 'binary_counting' && message.content === '/start'){
  //     var ServerJSON = JSON.parse(fs.readFileSync('../db/num.js'));
  //     ServerJSON[message.guild.id] = {
  //       numd: 1,
  //   };
  //   fs.writeFileSync('../db/num.js', JSON.stringify(ServerJSON));
  //   message.channel.send(`I'll start: 0000 0000 0000 00010`)
  // }else{
  //   console.log('hi')
  // }
}